spring:
  autoconfigure:
    exclude: net.devh.boot.grpc.server.autoconfigure.GrpcServerSecurityAutoConfiguration, org.springframework.boot.autoconfigure.jms.JmsAutoConfiguration

  cloud:
    discovery:
      enabled: false

web:
  user-service:
    url: http://localhost:8080
    users-endpoint: /api/users
    mentors-endpoint: /api/mentors
    mentees-endpoint: /api/mentees