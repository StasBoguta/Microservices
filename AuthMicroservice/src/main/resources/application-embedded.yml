server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  #SQLDatabase setting
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:/sql/data.sql

  main:
    allow-circular-references: true

  application:
    name: "auth-microservice"

management:
  metrics:
    export:
      prometheus:
        enabled: true

  endpoints:
    web:
      exposure:
        include: "*"

    enabled-by-default: true

  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true

sentry:
  dsn: https://ad0ba40f71274489a80b1c07e2fc6607@o4504249281085440.ingest.sentry.io/4504249282592768
  traces-sample-rate: 1.0

activemq:
  protocol: tcp
  host: rabbitmq
  port: 61616